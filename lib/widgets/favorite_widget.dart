import 'package:flutter/material.dart';
import 'package:store_app/widgets/popular_item_widget.dart';

class Favorite_widget extends StatelessWidget {
  const Favorite_widget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Column(
      children: [
        // ListView(
        //   children: [Polpular_item_widget()],
        // )

        // Polpular_item_widget(context)
      ],
    ));
  }
}
